---
trigger: always_on
---

only use mui and not tailwind unless spicifieed to use somthing else
copy the screenshots eexactly how they were
and alwayse make an obj or a json file that controls the site as if there was a backend sendign the data but its just a list locally